# Grafana Webhook Server 환경변수

# 웹훅 서버 포트 (기본값: 3000)
PORT=3000

# Slack Bot Token
# Slack 앱 설정 > OAuth & Permissions 에서 발급 (xoxb-로 시작)
# 필요 권한(Scopes): chat:write, chat:write.public, app_mentions:read, commands
SLACK_BOT_TOKEN=your-slack-bot-token-here

# Slack App Token (Socket Mode 전용)
# Slack 앱 설정 > Basic Information > App-Level Tokens 에서 발급 (xapp-로 시작)
# 필요 스코프: connections:write
SLACK_APP_TOKEN=xapp-your-app-token-here

# 메시지를 전송할 Slack 채널 ID
# 채널명이 아닌 ID (C로 시작하는 값, 채널 우클릭 > 링크 복사 하단에서 확인)
SLACK_CHANNEL_ID=C0XXXXXXXXX

# Grafana 웹훅 인증 토큰 (선택사항)
# Grafana Contact Point에서 동일한 값을 설정해야 함
# 설정하지 않으면 모든 요청을 허용 (내부망 사용시)
WEBHOOK_SECRET=your-secret-token-here

# Claude Code 프로젝트 디렉토리
# .claude/agents/ 폴더가 있는 경로
CLAUDE_PROJECT_DIR=/home/ec2-user/slackIncidentAgent
